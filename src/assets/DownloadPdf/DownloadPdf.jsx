import React, { useState } from 'react';

const DownloadPdf = ({  }) => {
  

  const downloadPdf = () => {
  
  };

  return (
    <>
      <input
        type="button"
        value="Download PDF"
        onClick={downloadPdf}
      />
    </>
  );
};

export default DownloadPdf;
